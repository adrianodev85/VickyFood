@model HomeViewModel

<div class="container">
    <div class="row text-center">
        <div>
            <h1>Vicky Food</h1>
        </div>
        <div>
            <partial name="_Carousel" />
        </div>
        <div>
            <h2>People`s Favorite</h2>
        </div>
        <div class="row">
            @foreach (var p in Model.FavoriteProducts)
            {
                <partial name="_ProductOverview" for="@p" />
            }
        </div>
    </div>
</div>